"""Small, dependency-light interpretability helpers.

The heavy phase-2 stack (torch/transformers/nnsight/TransformerLens) can build
on these helpers, but importing this package itself stays cheap and CPU-only.
"""

